/**
 * IStorage interface — single source of truth for all storage method contracts.
 * The concrete implementation lives in ./database-storage.ts.
 */
export type { IStorage } from "./database-storage";
