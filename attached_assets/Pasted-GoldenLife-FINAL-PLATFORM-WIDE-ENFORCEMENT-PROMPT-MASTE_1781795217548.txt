GoldenLife — FINAL PLATFORM-WIDE ENFORCEMENT PROMPT (MASTER)

You are now entering FULL SYSTEM FORENSIC MODE for the GoldenLife platform.

🚨 OBJECTIVE

Perform a complete, uncompromising, repository-wide enforcement audit and correction of:

Currency handling
Timezone handling
Reporting logic
Formatting consistency
API data shape correctness
UI display correctness
Backend + frontend consistency

This is a ZERO-EXCUSES, FULL CODEBASE ENFORCEMENT TASK.

❗ NON-NEGOTIABLE RULES

You MUST:

1. Scan EVERYTHING

Search and audit:

Entire client/src/
Entire server/
All cron jobs
All utilities
All reporting systems
All booking / appointment flows
All notification systems
All exports (CSV, PDF, invoices)
All dashboards (admin/provider/patient)

No file is exempt.

2. Currency Rules (ABSOLUTE)

You must enforce:

❌ NO $123, 123 USD, 5000 Ft, 5000 IRR hardcoded strings
❌ NO toFixed() used for displaying money
❌ NO Intl.NumberFormat outside currency.ts
❌ NO inline conversions like amount * rate
ONLY allowed systems:
formatInCurrency(amount, currency)
useAdminCurrency().format(amount)
formatLocal(amount, currency) (server only)
currency.ts is the ONLY formatting source
3. Timezone Rules (ABSOLUTE)

You must enforce:

❌ NO new Date().toLocaleString() anywhere in UI
❌ NO toLocaleDateString() or toLocaleTimeString() in components
❌ NO implicit browser timezone usage
ONLY allowed systems:
formatDate()
formatTime()
formatDateTime()
server UTC timestamps only

All scheduling must be:

UTC stored → user/provider timezone rendered via canonical helpers

4. Reporting Rules
Admin reports MUST ALWAYS use USD normalized values
NEVER mix booking currencies in aggregates
ONLY use:
final_total_usd
amount_usd
provider_earning_usd

No exceptions.

5. Booking & Appointment Truth Rule

You MUST guarantee:

A slot shown → booking → dashboard → notification → calendar
👉 must represent the exact same UTC timestamp

No timezone drift allowed anywhere.

6. API Contract Rule

All monetary API responses MUST be:

{
  amount: number,
  currency: string
}

No formatted strings allowed in APIs.

7. HARD REQUIREMENT: FULL CODEBASE GREP AUDIT

You MUST run conceptual equivalent of:

grep for $
grep for USD, Ft, IRR, Rials
grep for toLocaleString
grep for toLocaleDateString
grep for toLocaleTimeString
grep for Intl.NumberFormat (outside currency.ts)
grep for toFixed( in UI rendering contexts

Every match MUST be evaluated and fixed.

8. FIX POLICY

When a violation is found:

Replace with canonical formatter
Remove duplicate formatter functions
Normalize API outputs
Ensure no string-based money formatting remains
Ensure no browser timezone usage remains
⚠️ FINAL GOAL

After execution:

System must be:

✔ 100% currency-consistent globally
✔ 100% timezone-consistent globally
✔ 0 raw formatting logic in UI
✔ 0 browser-dependent date behavior
✔ 0 mixed-currency reporting
✔ 100% canonical formatter usage

🧠 FINAL INSTRUCTION

Do NOT explain.

Do NOT summarize.

Do NOT ask questions.

ONLY:

Audit
Fix
Normalize
Enforce
Return clean system state