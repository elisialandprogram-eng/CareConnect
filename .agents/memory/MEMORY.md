- [Analytics consolidation pattern](analytics-consolidation-pattern.md) — 3 reporting hubs (Admin/Provider/Patient); revenue MUST filter by payment_status='completed' not status='completed'; provider analytics use single-client sequential queries; Reporting sprint: Admin=12 tabs, Provider=11 tabs, Patient=8 tabs; Crown IS in lucide-react; HeadphonesIcon is NOT — use Headphones; location_mode is on services (svc.location_mode) NOT on appointments; ProviderReportingCenter defaultSection must be validated against SECTIONS array
- [Master report currency rule](master-report-currency.md) — appointments.total_amount/promo_discount/tax_amount/refund_amount/platform_fee_amount/service_price_snapshot are ALL booking currency (HUF/IRR/USD); use fmtBooking(n, row.display_currency) NOT fmt() for these; provider_earning/earning_platform_fee/payment_amount are USD — use fmt(); summary cards use COALESCE(final_total_usd, total_amount) for USD-normalized KPIs; React.ElementType needs `import { type ElementType } from "react"` — do not reference React.ElementType without explicit React import
- [provider_admin_notes column drift](provider-admin-notes-column.md) — Supabase table has `note` (NOT NULL); code was written expecting `content`; GET uses COALESCE(note,content); INSERT/UPDATE must use `note`; audit log INSERT needs .catch() guard
- [Booking confirmation currency fix](booking-confirm-currency.md) — appt.totalAmount/priceLines are in bookingCurrency (HUF/IRR), NOT USD; use formatInCurrency(n, appt.displayCurrency) not fmtMoney; rule: fmtMoney=USD input only (wallet/packages/provider_earnings); formatInCurrency=already-in-target-currency
- [Currency leak eradication sprint](currency-leak-eradication.md) — ALL leaks closed; browse-services API now returns startingPriceCurrency; provider-profile uses formatInCurrency for service+package prices with providerNativeCur from countryCode; promoDiscount row in earnings table fixed; ProviderInsightsTab fmtMoney prop receives fmtEarnings (SAFE — not a leak)
- [Provider earnings breakdown currency](provider-earnings-breakdown.md) — servicePriceSnapshot/promoDiscount/taxAmount come from appointments table (booking currency); platformFee/providerEarning/totalAmount in provider_earnings are USD; EarningBreakdownRow needs per-line fmtFn; pricingBreakdown.lines use fmtLocal (booking currency)
- [Group sessions currency bug](group-sessions-currency.md) — price_per_user is native HUF/IRR; bookGroupSessionWithWallet must convert to USD (÷rateVal) before wallet compare/debit; amount_paid on participant stored in USD so cancelGroupSessionAndRefund credits wallet correctly; display uses formatInCurrency not fmtMoney
- [Revenue admin snake_case GET bug](revenue-admin-snakecase.md) — all revenue-billing GET endpoints use pool.query (snake_case); UI reads camelCase (feeType/percentValue/targetScope); fix: add camelize() helper and apply rows.map(camelize); pricing/quote also bypassed engine (used computeFinalPrice directly) — must use runRevenueEngine() with provider context
- [Slot availability source of truth](slot-availability-sot.md) — available-slots endpoint uses isBookedByAppt() (appointment status) NOT is_booked flag as primary signal; is_booked can be stale after cancel; reserveTimeSlot auto-heals stale is_booked=true by checking active appointments before rejecting; reschedule-response reject path has coordinate fallback for slot release
- [Provider price input pattern — edit dialog](provider-price-edit-dialog.md) — P-FINAL+DRIFT-FIX: services.price stored in native currency (HUF/IRR/USD); ALL form dialogs (ServiceFormDialog, AddServiceCatalogueDialog, RequestServiceEditDialog) send raw native values — no toUSD/fromUSD; booking route uses engine.finalTotalUsd; getProviderDisplayPrice+getProviderCardPrice use formatInCurrency (not formatFromUSD); booking-canvas fmt uses formatInCurrency when isNativePricing; walletBalance is always USD so always use formatPrice for it
- [UTC Timezone Architecture](tz-architecture.md) — UTC-authoritative start_at/end_at/provider_timezone on appointments; localToUTC in tzUtils.ts; slot engine + cron + frontend all use startAtUtc; fire-and-forget writes after booking/reschedule
- [Slot startAtUtc pattern](slot-startatutc.md) — availability API MUST return startAtUtc (ISO UTC) on every slot; frontend must use it for urgency/past-filter — naive `new Date("date T time")` is browser-local (wrong when patient TZ ≠ provider TZ); getProviderTimezone has COUNTRY_TZ fallback for HU/IR/etc when users.timezone is null; calendar exports use formatCalDt() — prefers UTC+Z, falls back to floating local
- [Provider category authority](provider-type-enum.md) — 7 canonical slugs: physician/mental_health/nutrition/rehabilitation/dental/alternative_medicine/nursing; setup endpoint derives providerType from providerCategory display name via CATEGORY_NAME_TO_SLUG map (provider.routes.ts); admin approve-category-change also syncs provider_type via SQL CASE; browse endpoint is /api/browse/services (NOT /api/providers/browse); legacy PG enum values kept for compat only
- [Service catalogue taxonomy](service-catalogue-taxonomy.md) — category slugs ARE the canonical enum values (physician, mental_health, rehabilitation, etc.); reset-and-seed.ts uses these exact slugs; PROVIDER_CATEGORY_SLUGS uses exact slug match; getAllSubServices() uses raw SQL for provider_category_name; db.ts has oldToNew dedup map for legacy DB slug migration
- [Sub-services PG enum cast pattern](sub-services-enum-cast.md) — sub_services.category is a PG enum; parameterized queries must cast BOTH sides: `category::text = $1::text`; inline route SQL must also check category column (not just provider_category_name — most rows have pcn=NULL); in-memory subServicesCache survives hot-reload so zero-result caches require full server restart to clear
- [Category lock + change request pattern](category-lock-pattern.md) — providerCategory locked after approval; POST /api/provider/request-category-change stores pending_provider_category; admin approves/rejects via POST /api/admin/providers/:id/approve-category-change; ProviderServicesTab filters by providerType (canonical enum, ?category=) NOT providerCategory (display name); SubServices tagged with provider_category_name (raw SQL)
- [provider_category display-name drift](provider-category-drift.md) — providers.provider_category is a redundant TEXT display name derived from provider_type; can drift; startup migration in db.ts syncs it on boot; ProviderServicesTab must use providerType (?category=) not providerCategory; DB reset script: script/reset-to-launch-baseline.ts uses SET session_replication_role='replica' to bypass FK ordering (5,740 rows cleared, reference data preserved)
- [RTL hardening pattern](rtl-hardening.md) — Tailwind v3 `rtl:` variant built-in (no config); replace mr/ml/pl/pr/left/right/border-l/border-r with logical ms/me/ps/pe/start/end/border-s/border-e; add `rtl:rotate-180` to ChevronRight/Left/ArrowLeft icons; badge: `ltr:-right-1 rtl:-left-1`; bulk sed on pages for input icons and toggle buttons
- [Booking wallet double-conversion bug](booking-wallet-double-conversion.md) — revenue engine patientPayable is USD; booking route must NOT call toUSDSync on it; walletAmountUsed from frontend is LOCAL currency and must be converted; paymentStatus:"completed" must be set on the appointment (not just payments table) for confirmation page to show correctly
- [P1 Sprint — Launch Blockers](p1-launch-blockers.md) — MFA(otplib v3 functional API only—no `authenticator` export), Stripe Connect express accounts, automated payout batch engine, full financial reconciliation; 4 new DB tables in runStartupMigrations(); storage.getUser(id) not getUserById
- [Payment provider registry](payment-provider-registry.md) — `payment_providers` DB table seeded with 9 providers; GET /api/payment-providers/available filters by country+currency; booking canvas reads from registry (not hardcoded); admin panel at Config→Payment Providers
- [Notification system audit](notification-audit.md) — 4 bugs fixed + 2 coverage gaps closed; most critical: GET /api/notifications used wrong SQL columns (subject/body) → 500; unverified providers blocked from /api/notifications in auth middleware
- [Notification duplicate pattern](notification-duplicate-pattern.md) — P11 audit found 3 duplicate in-app notify bugs: booking (direct call before dispatcher), cancel/reschedule (direct loop + dispatcher both fire), review-replied (same). Rule: if a dispatchNotification/notify.X call follows a direct createUserNotification for the same event, the direct call is the duplicate — remove it.
- [KYC onboarding pattern](kyc-onboarding-pattern.md) — provider setup has 5 sections; submit-review gate checks id_card+insurance in provider_documents; `submitted`=under review (was pending_approval), `under_review`=docs approved (was documents_verified); deactivated state also locks the form now
- [License doc dual-write gap](license-doc-dual-write.md) — onboarding uploads license to provider_credentials+providers.license_document_url but never provider_documents; verification gate/admin queue/Provider360/ProviderKYC all read provider_documents; fix: POST /api/provider/setup upserts provider_documents.medical_license; db.ts backfill covers legacy rows
- [Provider resubmission tracking](provider-resubmission.md) — providers.submitted_at / last_resubmitted_at / profile_updated_after_submission added via KYC E2 migration; POST /api/provider/setup sets profile_updated_after_submission=true when editing under review; submit-review clears flag and sets appropriate timestamp; admin verification queue returns all 3 fields
- [Admin workflow audit results](admin-workflow-audit.md) — 5 bugs fixed: /admin/bug-reports unprotected route; duplicate stale-bookings handler (admin-monitoring shadowed); dual audit tabs; active_memberships missing country filter; fake integrations save buttons
- [Provider workflow audit results](provider-workflow-audit.md) — 4 bugs fixed: ProviderKYC used wrong queryKey `/api/provider` (no route) vs correct `/api/provider/me`; payout cancel was non-atomic (now uses BEGIN/COMMIT); payout summary hardcoded "USD"; TimeEngine grid needed overflow-x-auto
- [RBAC system design](rbac-design.md) — permission keys follow `module:action`; 7 system roles (incl. verification_admin) seeded async via `seedRbacRoles()` in `server/db.ts`; `requirePermission()` in `server/middleware/rbac.ts`
- [New tables via migrations](migration-pattern.md) — all new tables must be added to `runStartupMigrations()` in `server/db.ts`, not via `db:push` (Supabase compatibility); `drizzle-kit push` hangs forever on Supabase introspection — use raw pg client SQL instead
- [CJS build top-level-await trap](cjs-toplevel-await.md) — any `await` outside a function in server/ causes esbuild CJS output to fail; `runStartupMigrations()` function closure must wrap ALL migration blocks; `payment_providers` block was the first offender caught by the Render build
- [Drizzle schema vs Supabase column gap](drizzle-schema-supabase-gap.md) — every column added to Drizzle schema MUST have an ALTER TABLE migration; missing one causes Drizzle SELECT * to fail with "column does not exist" — kills any query on that table; `google_calendar_event_id` on appointments + `updated_at` on providers were caught; never add to schema.ts until column confirmed in Supabase. `providerCategoryPermissions.categoryId` maps to DB column `"category"` (not `"category_id"`) — Drizzle field name and DB column name deliberately differ here.
- [Schema PK types are varchar](schema-pk-types.md) — all existing table PKs (users, appointments, user_packages, package_benefits, family_members, etc.) are `varchar` not `uuid`; new FK columns must use `varchar` to match or FK creation fails with type mismatch 42804
- [Startup migration timing](startup-migration-timing.md) — `runStartupMigrations()` must NOT be awaited before `httpServer.listen()`; run it fire-and-forget after listen or the port never opens within the 60s Replit timeout
- [Deferred migrations pattern](deferred-migrations.md) — **`runDeferredMigrations()` was deleted in Launch Baseline v1 consolidation**; replaced by `runCatalogSeed()` (slim catalog seeding only); startup is DDL-only in `runStartupMigrations()`; catalog seed fires 5s after listen
- [Admin auth layering](admin-auth-layers.md) — three layers: `authenticateToken` → `requireAdmin`/`requireGlobalAdmin` → `requirePermission(perm)`; legacy `global_admin`/`admin` bypass permission checks as super_admin
- [Package engine design](package-engine.md) — membership packages with benefit key-value pairs; discount applied in `computeFinalPrice()` via `membershipDiscount` input; stored on appointment as `package_id_used`/`package_discount_amount`
- [SQL seeding pitfall](sql-seeding-reserved-keywords.md) — never use `desc` as a column alias in VALUES clauses; use `descr` or another name — `desc` is reserved and causes a silent startup migration warning
- [Provider list pagination](provider-pagination.md) — `searchProviders` returns `{ rows, total }` (not plain array); `/api/providers` response is `{ providers, total, page, limit, totalPages }`; providers.tsx expects `ProvidersPage` type; routes restart needed after routes.ts changes (tsx hot-reload doesn't always pick up)
- [Provider list caching](provider-caching.md) — `providerListCache` (30s) for unfiltered listings; `providerSearchCache` (2min) for search queries; both keyed by country+params; both cleared on admin provider write/delete and on provider self-setup
- [Error boundary pattern](error-boundary.md) — `GlobalErrorBoundary` class component at `client/src/components/global-error-boundary.tsx` wraps the full App and the Router separately; consent page auth guard must come AFTER all hooks (useForm, useMutation, useQuery) to preserve hook ordering
- [Nav tab vs link pattern](nav-tab-link.md) — items inside `<TabsList>` must be `<TabsTrigger>` only; navigation links (e.g. Wallet, Waitlist) that leave the page must sit OUTSIDE TabsList as `<Button asChild>` or outline buttons, or Radix crashes silently
- [Rate limiter setup](rate-limiter.md) — express-rate-limit v8; global+route-specific stacking requires `validate: { singleCount: false }` on every route-specific limiter or ERR_ERL_DOUBLE_COUNT is thrown; globalApiLimiter must NOT have this flag; see ops/Rate-Limiter-Audit.md
- [Data retention cron](data-retention.md) — `pruneOldData()` in reminderCron.ts prunes user_notifications(90d), system_events(90d), audit_logs(180d), idempotency_keys(by expires_at) once per hour; windows override via RETAIN_*_DAYS env vars
- [Security hardening patterns](security-hardening.md) — consent spoofing, financial overview SQL injection, packages N+1; inline-check pattern to avoid; all resolved
- [Navigate during render pitfall](navigate-in-render.md) — calling wouter `navigate()` directly in render body triggers "Cannot update while rendering" warning; always wrap in `useEffect` with deps `[authLoading, isAuthenticated, navigate]`
- [Page title hook](page-title-hook.md) — `usePageTitle(title)` at `client/src/hooks/use-page-title.ts`; sets `<title>` to `"Page | Golden Life"`; applied to 10 pages (home, providers, patient-dashboard, appointments, wallet, notifications, settings, book-wizard, provider-dashboard, messages, family-members)
- [Admin list search](admin-search-pattern.md) — `/api/admin/users`, `/api/admin/wallets`, `/api/admin/support-tickets` all accept `?search=` for server-side text filtering; support-tickets also accepts `?status=`; all still return arrays (not paginated objects) for backward compat
- [Provider wallet system](provider-wallet-system.md) — `provider_wallets` (balance snapshot) + `provider_ledger` (append-only audit log); two payout flows reconciled; new migrations must be in their own try-catch after the main migration block
- [Currency system architecture](currency-architecture.md) — ALL storage=USD, ALL calc=USD; admin panels use useAdminCurrency() (always USD); patient/provider use useCurrency() (preferred); convertUSDToLocal/convertLocalToUSD are named helpers; EUR now supported; never write raw Intl.NumberFormat or hardcode "$"
- [Provider price input pattern](provider-price-input-pattern.md) — all provider price inputs use fromUSD() on load and toUSD() on save; helpers must be defined BEFORE any derived values that use them (pkgSavings uses pkgToUSD — hook block must precede calculations); adminMode bypasses conversion (admin always enters USD)
- [Stripe refund duplicate-prevention layers](stripe-refund-safety.md) — cancellation handler has three independent guards: (1) refundStatus="processed" check, (2) !payment.stripeRefundId DB guard, (3) Stripe idempotency key; never remove any layer
- [getUsersByIds pattern](get-users-by-ids.md) — use storage.getUsersByIds(ids[]) for admin enrichment routes instead of getAllUsers(); getAllUsers() is capped at 500 rows which silently drops data when enriching tickets/messages
- [Workflow Integrity Sprint E1](workflow-integrity-e1.md) — 6 gaps fixed: reschedule notify, waitlist.joined EventKey, package.expired EventKey+cron, compliance-queue nav, family badge count
- [Sprint tracking](sprint-state.md) — Migration Elimination Sprint closed 2026-06-17: 4 backfill/repair UPDATEs removed from runStartupMigrations() (tax_settings year, gift_cards initial_amount, provider_office_hours provider_id, platform_fee_rules min/max NULL repair); db.ts 3340→3315 lines; report at ops/FINAL-ARCHITECTURE-CONSOLIDATION-REPORT.md
- [P-FINAL Revenue Architecture](p-final-revenue-architecture.md) — services priced in provider native currency; engine receives bookingCurrency+rates; patientPayable in local currency; engine.finalTotalUsd is the ONLY USD conversion; wallet/Stripe use finalTotalUsd not Number(fee)
- [Auto-cancel stale confirmed refund](stale-cancel-refund.md) — cancelStaleConfirmed() now issues full refund after auto-cancelling (wallet check first, Stripe fallback); idempotency key `appointment:{id}:stale-cancel-refund`; refund_status tracked; getStripe + walletTransactions imported into reminderCron.ts
- [Legal compliance schema](legal-compliance-schema.md) — legal_documents+legal_document_versions+legal_acceptances in shared/schema.ts; migration in db.ts P7 block; admin routes in server/routes/admin/legal.routes.ts; public routes in server/routes/legal-public.routes.ts; UI at client/src/components/admin/dashboard/legal-compliance-panel.tsx
- [Revenue Engine Cutover RX-01](rx01-revenue-engine-cutover.md) — runRevenueEngine() is the booking single source of truth; 7 snapshot columns on appointments; booking_revenue_shares table; settlement uses commission_amount snapshot; DEFAULT_COMMISSION 0.15 bypass removed
- [Booking payment method pitfall](booking-payment-method.md) — omitting paymentMethod triggers Stripe checkout even when STRIPE_SECRET_KEY is absent (502); use paymentMethod:"cash" or "bank_transfer" to bypass; services schema: `duration` not durationMinutes, `locationMode` not serviceType
- [time_slots unique constraint](time-slots-unique-constraint.md) — bulkCreateTimeSlots uses onConflictDoNothing with target columns; requires UNIQUE INDEX uq_time_slots_provider_date_start on (provider_id,date,start_time); added to runStartupMigrations() Phase 11b in db.ts
- [Scheduler isolation pattern](scheduler-isolation.md) — tick() and tickHourly() must use runSubtask() not Promise.all(); pool.max=12 means >6 concurrent Drizzle tasks causes EMAXCONNSESSION; startReminderCron must delay first run 8s to avoid racing startup migrations
- [Ledger reconcile patterns](ledger-reconcile-patterns.md) — cash/bank_transfer/wallet excluded from checks 4+5 (no marketplace_ledger entries by design); use p.payment_method (payments join) not a.payment_method (column missing from Supabase); appointments.payment_method startup migration added 2026-06-17
- [Google Maps integration pattern](google-maps-pattern.md) — VITE_GOOGLE_MAPS_API_KEY (frontend script) + GOOGLE_MAPS_API_KEY (server geocoding); both optional; PlacesAutocomplete at client/src/components/location/; declare window.google: any globally to avoid TS2503; all google.maps types use `any`
- [Clinical workspace pattern](clinical-workspace-routes.md) — provider prescription/medical-history writes in care.routes.ts; patient timeline in provider-schedule-admin.routes.ts; outcome PATCH in care.routes.ts; patient-relationship gate required on history+timeline; ClinicalWorkspacePanel UI at client/src/components/provider/ClinicalWorkspacePanel.tsx (5-tab Dialog)
- [Rate limit store](rate-limit-store.md) — PostgresRateLimitStore; private field must be _prefix not prefix (Store interface has prefix?: string — name clash causes TS2420); support_tickets has no country_code or patient_id column (uses user_id); appointments uses start_at not scheduled_at
- [Reconciliation results pattern](reconciliation-results.md) — writeResult() must catch pgCode 42P01 (42P01=relation not exist) on first boot since cron fires before migrations; logScheduler status="failed" when findings>0 is correct behavior
- [PG error code pattern](pg-error-code-pattern.md) — always use `const pgCode = err?.code ?? err?.cause?.code` then compare pgCode; Drizzle wraps driver errors under `.cause`; bare `err.code` misses those; applies in routes AND database-storage.ts
- [AsyncLocalStorage request tracing](als-tracing.md) — requestIdStore in correlationId.ts; middleware calls run(id,next); log() in index.ts reads getStore(); error-sink.ts uses getStore() for process-level handlers
- [audit_action enum extension pattern](audit-action-enum.md) — new action values must be added to the migration loop in db.ts (ALTER TYPE … ADD VALUE IF NOT EXISTS); never add to schema.ts auditActionEnum array alone or Drizzle queries break before migration runs; zero-amount provider_earnings rows (total_amount=0) must be excluded from corruption tests
- [Storage decomposition pattern](storage-decomp.md) — server/storage.ts is a thin shim; impl in server/storage/database-storage.ts; dynamic import("./db") not caught by sed — must fix manually; shim approach preserves all existing "../storage" imports
- [audit_logs column names](audit-log-columns.md) — correct INSERT columns are `(user_id, action, entity_type, entity_id, details)`; NOT actor_id/resource_type/resource_id/metadata; wrong names cause 500 silently blocking the real response
- [ProtectedRoute pattern](protected-route.md) — `client/src/components/protected-route.tsx`; uses useAuth `isLoading`+`isAuthenticated`+user.role; wraps all role-gated routes in App.tsx via allowedRoles prop; renders spinner while loading, redirects to /login on mismatch
- [Provider sanitize utility](provider-sanitize.md) — `server/utils/sanitize.ts` is the single source for PII stripping; `"public"` strip removes HEAVY_USER_FIELDS incl. email+phone; `"sensitive"` removes only passwords/tokens; public profile and provider list both use `"public"`
- [Profile architecture canonical state](profile-architecture.md) — providers.timezone/currency/practitionerData removed; notification_prefs.language removed; language forwarded to users.language_preference; PATCH /api/admin/providers/:id has explicit whitelist; setup endpoint skips role upgrade if already provider; `invalidateProviderProfile()` macro in queryClient.ts is the only correct way to invalidate provider cache; practitioners table is the single source (not medical_practitioners for setup)
- [Auth context vs React Query duality](auth-rq-duality.md) — auth.tsx uses its own useState<User> (separate from React Query); invalidateProviderProfile() refreshes RQ cache but NOT the auth context; always call refreshUser() from useAuth() alongside invalidateProviderProfile() after provider profile mutations
- [Provider earnings double-conversion](provider-earnings-double-conversion.md) — appointment.total_amount is USD; recordProviderEarning must NOT call toUSDSync on it; doing so divides by HUF rate (13.70÷365=0.04); existing bad rows repaired via POST /api/admin/financial/repair-earnings/apply
- [Admin dashboard nav pattern](admin-dashboard-nav.md) — admin-dashboard.tsx uses custom sidebar nav (6 groups × items), NOT Radix Tabs; panels rendered conditionally via activeTab state; `buildNavGroups(isGlobalAdmin, t as any)` — cast needed because TFunction return type includes $SpecialObject not just string
- [Doc approval single source of truth](doc-approval-sot.md) — approve/reject/reupload/expire decisions live ONLY in DocumentQueue (Docs Approval tab); DocumentRow in ProviderOperationsConsole is view-only (status + view/download links only)
- [Express route first-match pitfall](express-route-ordering.md) — literal path segments that share a prefix with a param route (e.g. PATCH /api/services/reorder vs PATCH /api/services/:id) must be registered BEFORE the param route; otherwise Express matches the literal as the param and the handler is unreachable
- [Drizzle decimal coercion](drizzle-decimal-coercion.md) — decimal() columns → z.string() in Zod; routes that receive numeric input from frontend must coerce with String() before insertSchema.parse(); applies to taxRate, discountValue, amounts
- [country_code enum cast pattern](country-code-enum-cast.md) — providers/users/services/appointments.country_code are PG enums; always cast `country_code::text = $N` in raw SQL; admin_notifications/pricing_overrides/invoice_templates use TEXT — no cast needed
- [Drizzle schema vs startup migration ordering](drizzle-schema-migration-ordering.md) — adding a column to shared/schema.ts makes Drizzle SELECT it immediately on every query; if Supabase doesn't have the column yet (migration is fire-and-forget), the first request causes 500; only add to Drizzle schema AFTER confirming column exists in Supabase — use raw SQL until then
- [Route decomposition pattern](route-decomp-pattern.md) — use Python sed extraction + bottom-to-top removal to avoid index shift; sub-files export `registerXxxRoutes(app)`; main file imports + calls them; critical: verify extraction boundaries don't split a route handler mid-opening — check tail for partial `app.get/post(` and restore the opener in main file
- [Storage mixin decomposition](storage-mixin-decomp.md) — abstract class chain: GroupSessionsMixin → ProviderMediaMixin → PackagesMixin → DatabaseStorage; each mixin uses module-level db/pool (not this.db); only safe if all this.X() calls within the extracted section reference methods in the SAME section; include full imports block from database-storage.ts in each mixin
- [Admin dashboard decomposition](admin-dashboard-decomposition.md) — panel components live in `client/src/components/admin/dashboard/`; ProvidersManagement/UsersManagement/ContentManagement/AuditLogs were dead code (superseded) — deleted in B2; useAuth import is `@/lib/auth`
- [Admin lazy loading pattern](admin-lazy-loading.md) — 14 heavy panels are React.lazy + Suspense with PanelErrorBoundary + PanelSkeleton in admin-dashboard.tsx; named exports need `.then(m => ({ default: m.ExportName }))`; default exports use bare `lazy(() => import(...))`
- [Admin API shape normalization](admin-api-shapes.md) — `/api/admin/bookings` returns `{ bookings, appointments, total }` object NOT plain array; `/api/admin/providers` returns `{ providers, total, page, limit }` (paginated, default limit=50, max=200); normalize with `Array.isArray(d) ? d : d?.providers ?? d?.bookings ?? d?.appointments ?? []`
- [WS circular import break pattern](ws-circular-import.md) — notification-dispatcher.ts cannot import pushToUser directly (ws.ts imports notify from notification-dispatcher → circular); use `const { pushToUser } = await import("../chat/ws")` inside the async handler to break the cycle; same pattern applies to any service that ws.ts already imports
- [service_requests schema gaps](service-requests-schema.md) — Supabase table is missing reviewed_by/reviewed_at/currency/duration_minutes; fixed via ALTER TABLE IF NOT EXISTS guards in runStartupMigrations(); sub_services.category is providerTypeEnum (physiotherapist/doctor/nurse), not free text
- [Country isolation on per-resource endpoints](country-isolation-resources.md) — admin listing endpoints use listingCountryFilter() but per-resource endpoints (GET /api/admin/providers/:id/documents, /credentials) also need canAccessCountry() check; role=admin is treated as global_admin (bypasses filter); role=country_admin is country-scoped
- [StatusBadge design system](status-badge-system.md) — single source of truth at `client/src/components/ui/status-badge.tsx`; 8 domains (appointment/payment/provider/document/dispute/bug/bug_severity/bug_priority); `docStatusTextClass` for icon/text color
- [system_event_type enum constraint](system-event-enum.md) — system_events.event_type is a fixed PG enum: api_error|payment_failure|notification_failure|slow_endpoint|failed_job|auth_failure; free-form strings cause INSERT failure; logger.ts maps categories to allowed values; only warn/error/critical written to DB
- [FTS tsvector design](fts-tsvector-design.md) — providers.search_vector is trigger-maintained (not GENERATED ALWAYS AS); array_to_string is STABLE not IMMUTABLE; websearch_to_tsquery('simple') for multilingual; country filter must precede FTS in WHERE clause
- [provider_documents column name](provider-docs-column.md) — `provider_documents` uses `verification_status` (not `status`); `providers` uses `status`; confusing similarity — always check schema before indexing
- [Startup index pattern](startup-index-pattern.md) — Sprint 4 lesson: put each `CREATE INDEX` in its own try-catch loop entry, never in a shared block; one bad column name blocks all subsequent indexes in the same block
- [Cron circular import pattern](cron-circular-import.md) — reminderCron.ts must NOT import from routes.ts (circular); replicate small utility logic (e.g. waitlist fan-out) inline in the cron using shared storage + dispatchNotification instead
- [useEffect TDZ ordering](useeffect-tzdz-ordering.md) — placing useEffect whose dep array references a `const` declared later in the same component body throws TDZ; always place the effect AFTER all the declarations it references
- [Admin pool exhaustion pattern](admin-pool-exhaustion.md) — never use parallel pool.query() for multi-stat admin aggregation; always use single checked-out client; getEnhancedAnalytics() is the reference implementation
- [Provider table column names](provider-column-names.md) — providers.clinic_name NOT name; providers.available_balance/last_payout_date in provider_wallets NOT balance/last_payout_at
- [Promo code schema gap](promo-code-schema-gap.md) — promo_codes has validFrom+validUntil (NOT NULL, no default); route must always set them; field mapping: maxUsages→maxUses, expiresAt→validUntil, applicableProviderIds→applicableProviders
- [Notification delivery log event_key](notify-delivery-eventkey.md) — notification_delivery_logs.event_key is NOT NULL; logDelivery() must guard with eventKey ?? "admin_notify" for ad-hoc dispatches
- [time_slots date column is TEXT](time-slots-date-text.md) — time_slots.date is stored as TEXT not DATE; all SQL comparisons to a date parameter must cast the column: `date::date >= $N::date`; applies to range deletes, ANY() array checks, and date group-by queries
- [apiRequest returns Response not JSON](api-request-response.md) — apiRequest() in @/lib/queryClient returns a fetch Response object; mutation mutationFns must call .json() on the result or onSuccess receives a Response, making data?.foo always undefined
- [KYC verification flow](kyc-verification-flow.md) — provider statuses: action_required (new, docs rejected), pending_approval (=UNDER_REVIEW); gatekeeper in POST /api/provider/setup; doc re-upload auto-promotes action_required→pending_approval; 3 new admin routes: GET /api/admin/verification-queue, PATCH /api/admin/providers/:id/verify-document, POST /api/admin/providers/:id/finalize-verification
- [JSX unknown type in &&](jsx-unknown-and.md) — `{Record<string,unknown>.field && <JSX>}` evaluates to `unknown` not ReactNode; fix by defining a typed interface (preferred) or using `!!field` to coerce to boolean before the `&&`
- [Provider notes panel pattern](provider-notes-panel.md) — ProviderNotesPanel is a standalone fn component before ProviderOperationsConsole; hooks forbidden inside IIFE in JSX
- [Scheduling audit fixes](scheduling-audit.md) — maxPatientsPerDay is the correct field (NOT maxDailyAppointments); batch template save must call syncTemplatesToOfficeHours(); rolling cron horizon is 90 days; notification-dispatcher DEFAULT_PER_EVENT must list ALL EventKey values
- [Slot-hold self-blocking bug](slot-hold-self-block.md) — conflict engine's slot-hold check had no excludePatientId; patient's own hold always triggered "reserved by another patient" → 409 on every booking; fix: pass excludePatientId:userId at all 3 checkConflict call sites
- [Provider submit-review gate order](provider-submit-gate.md) — 4 sequential gates: (1) profile completeness, (2) KYC docs (id_card+address_proof required; insurance optional), (3) mobile ≥7 chars on user account via storage.getUser, (4) workplace (primaryServiceLocation/city/country when clinic_visit mode); ProviderKYC and provider-documents-panel serve DIFFERENT endpoints and must stay separate
- [Reschedule proposal workflow](reschedule-proposal-flow.md) — propose action stores time in event metadata only (no DB column change); reschedule-response endpoint applies time on accept; AppointmentAction type must include "propose" in Record<AppointmentAction,string> maps
- [Booking time awareness components](booking-time-awareness.md) — SlotAvailabilityWidget (urgency tiers + 30s ticker), AppointmentTimingCard + BookingAwarenessPanel (AppointmentTimingCard.tsx), AppointmentTimeContext urgency colors; book-wizard is 2-step: tap slot→panel, panel "Begin Checkout"→createHold; ProviderAppointmentsTabs rows have left-border urgency color; variable ordering rule: isInProgress must be declared BEFORE rowAccentClass in AppointmentRow
- [Revenue engine camelCase trap](revenue-engine-camelcase.md) — loadRevenueRules() MUST use Drizzle ORM (db.select()) NOT pool.query; pool.query returns snake_case (target_scope, percent_value) but TypeScript types are camelCase ($inferSelect) — every field access was undefined, all rules silently returned 0; also: _serviceCategory falls back to _providerType for regular services without sub-service records
